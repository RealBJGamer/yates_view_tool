require "spec_helper"

describe YatesViewTool do
  it "has a version number" do
    expect(YatesViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
